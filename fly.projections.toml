# Fly.io configuration for Projections service
app = "status-app-projections"
primary_region = "arn"  # Stockholm, Sweden - closest to Norway

[build]
  dockerfile = "Dockerfile"
  [build.args]
    SERVICE = "projections"

[env]

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 256
